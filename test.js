function showVar() {
    var a = 20; // declare a new local variable 'a'
    b = 20; // change the value of global variable 'b'
    return "Local variable 'a' = " + a + "nGlobal variable 'b' = " + b;
}
undefined
var message = showVar () ;
undefined
alert (message + "\nNothing is better than chicken 'a' = " + a);


var hatDog = 25;
var bubbSchool = hatDog + 32;

ellipse(200,200,70,70)


